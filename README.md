
## 🚀 Запуск бота

Бот запускается в Docker-контейнере и конфигурируется через переменные окружения.

### Быстрый запуск

```bash
sudo docker build -t tg_bot .
sudo docker run \
  -e BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN \
  -e ACL_IDS=ID1,ID2,IDn... \
  -e ADMINS_IDS=ID1,ID2,IDn... \
  -e ZBX_URI=.. \
  -e ZBX_USER=zabbix \
  -e ZBX_PASSWD=syperstrongpass \
  tg_bot
```


## 📁 Структура проекта

```
tg_bot_dev/
├── app.py # Точка входа бота (инициализация Bot, Dispatcher, роутеров)
├── settings.py # Загрузка переменных окружения, конфигурация Settings
│
├── handlers/ # Обработчики Telegram-сообщений
│ ├── admin_handler.py # Админ-зона: ACL (добавление/удаление пользователей)
│ ├── navigation.py # /start, переходы между меню
│ ├── zabbix_handler.py # Команды "Alerts за N дней"
│ └── init.py # Экспорт роутеров
│
├── keyboards/ # Клавиатуры (ReplyKeyboardMarkup)
│ ├── main_keyboard.py # Главное меню
│ ├── admin_keyboard.py # Меню админ-зоны
│ └── init.py
│
├── services/ # Логика взаимодействия с внешними системами
│ ├── parser.py # Работа с Zabbix API (поиск проблем за N дней)
│ └── init.py
│
├── states/ # FSM состояния aiogram для пошаговых сценариев
│ ├── add_user_state.py # Состояние: ввод ID для добавления
│ ├── del_user_state.py # Состояние: ввод ID для удаления
│ └── init.py
│
├── utils/ # Утилиты и вспомогательные функции
│ ├── acl.py # Работа с ACL, добавление/удаление пользователей
│ └── init.py
│
└── init.py
```

## 🛠 Используемые инструменты

### Основные библиотеки
- **Python 3.12+** — основной язык разработки.
- **aiogram 3.x** — асинхронный фреймворк для Telegram Bot API.
- **python-dotenv** — загрузка переменных окружения из `.env`.
- **pyzabbix** — клиент для работы с Zabbix API.

### Инфраструктура
- **Docker** — удобная упаковка и запуск бота в контейнере.
- **Zabbix API** — источник данных (получение активных проблем и событий).

### Дополнительно
- **FSM от aiogram** — реализация состояний (добавление/удаление пользователей).
- **ReplyKeyboardMarkup** — удобная кнопочная навигация в Telegram.
- **logging** — структурированные логи запуска и работы бота.

Все зависимости собираются через `requirements.txt` и автоматически попадают в Docker-образ.



